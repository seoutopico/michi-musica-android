<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
    <application android:label="Michi · validación" tools:replace="android:label">
        <activity android:name=".validation.PodcastRegressionActivity" android:exported="true" />
        <provider android:name=".validation.RegressionDocuments" android:authorities="${applicationId}.documents" android:exported="true" android:permission="android.permission.MANAGE_DOCUMENTS" android:grantUriPermissions="true" />
    </application>
</manifest>
